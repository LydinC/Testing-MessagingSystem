------------------------------READ ME---------------------------------
The path specified in the project WebTesting,specifically in the test/java/cucumber/stepdefs/WebAppStepDefs should point to the 
chromedriver.exe installed on the machine.
Note that if jenkins is installed, the browser will not properly
open the main page when the url is set to 127.0.0.1:8080/mainpage.jsp
======================================================================